{
  "name": "class-unblocker",
  "version": "1.0.0",
  "description": "A reverse proxy that bypasses site restrictions, running entirely on Vercel.",
  "main": "api/proxy.js",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "engines": {
    "node": ">=18.x"
  },
  "license": "MIT"
}
