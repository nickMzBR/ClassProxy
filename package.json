{
  "name": "class-unblocker",
  "version": "1.0.0",
  "description": "Web proxy powered by Ultraviolet — deploy on Vercel",
  "private": true,
  "scripts": {
    "dev": "npx serve public -l 3000",
    "build": "echo 'Static project — no build step required.'"
  },
  "dependencies": {
    "@titaniumnetwork-dev/ultraviolet": "^2.0.0"
  },
  "devDependencies": {
    "serve": "^14.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
